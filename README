SVG Widget Library
------------------

Q: There are many SVG Widget libraries. Why create a new one?
A: Partly because this is a learning project for me. But mostly because the existing libraries didn't do quite what I wanted.
